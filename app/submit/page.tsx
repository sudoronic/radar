import { submitOpportunity } from "@/app/engagement/actions";
import { Button, Input, Select } from "@/components/ui";
import { CATEGORIES } from "@/lib/catalog";
export const metadata={title:"Submit an opportunity"};
export default function Submit(){return <main id="content" className="auth-wrap"><form action={submitOpportunity} className="card submit-form"><p className="eyebrow">HELP RADAR GROW</p><h1>Suggest an opportunity</h1><p className="muted">Every submission is reviewed before it appears publicly.</p><label>Title<Input required name="title" maxLength={200}/></label><label>Organizer<Input name="organizer" maxLength={150}/></label><label>Category<Select name="category">{CATEGORIES.map(c=><option key={c.slug} value={c.slug}>{c.name}</option>)}</Select></label><label>City<Input required name="city"/></label><label>Event date<Input name="event_at" type="datetime-local"/></label><label>Registration deadline<Input name="registration_deadline" type="datetime-local"/></label><label>Registration URL<Input name="registration_url" type="url"/></label><label>Source URL<Input required name="source_url" type="url"/></label><label>Description<textarea name="description" maxLength={12000}/></label><Button>Send for review →</Button></form></main>;}
